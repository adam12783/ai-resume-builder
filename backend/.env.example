# ===========================================
# AI Resume Builder - Backend Environment Variables
# ===========================================

# Server Configuration
PORT=3000
NODE_ENV=production
FRONTEND_URL=https://your-dashboard.vercel.app

# JWT Secret (generate a secure random string)
JWT_SECRET=your-super-secret-jwt-key-change-this

# OpenAI API Key (required for CV generation)
OPENAI_API_KEY=sk-your-openai-api-key

# Admin Account (created on first startup)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=your-secure-admin-password

# ===========================================
# Database Configuration (choose one)
# ===========================================

# Option 1: PostgreSQL (Supabase/Neon/Railway) - RECOMMENDED for production
DATABASE_URL=postgresql://user:password@host:5432/database

# Option 2: SQLite (local development only)
# Leave DATABASE_URL empty to use SQLite

# ===========================================
# Cloud Storage Configuration (choose one)
# ===========================================

# Storage Provider: 'supabase' or 'r2'
STORAGE_PROVIDER=supabase

# --- Supabase Storage (RECOMMENDED - free 1GB) ---
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your-supabase-service-role-key
SUPABASE_BUCKET=resumes

# --- Cloudflare R2 Storage (alternative - free 10GB) ---
# R2_ACCOUNT_ID=your-cloudflare-account-id
# R2_ACCESS_KEY_ID=your-r2-access-key
# R2_SECRET_ACCESS_KEY=your-r2-secret-key
# R2_BUCKET_NAME=resumes
# R2_PUBLIC_URL=https://your-r2-bucket.your-domain.com

# ===========================================
# Cleanup Configuration
# ===========================================

# Number of days to keep files (default: 60 = 2 months)
FILE_RETENTION_DAYS=60

# Enable internal cron job (set to 'true' for Render, 'false' if using external cron)
ENABLE_CRON_CLEANUP=true

# Secret for external cleanup trigger (optional, for security)
CLEANUP_SECRET=your-cleanup-secret-key
